# dependencies
node_modules
.pnpm-store

# env
.env
.env.local
.env.*.local
!.env.example

# build outputs
dist
out
.next
.turbo
build

# foundry
packages/contracts/out
packages/contracts/cache
# broadcast logs — deploy provenance lives in DEPLOYMENTS.md, not raw JSON
packages/contracts/broadcast/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editors
.vscode
.idea
.DS_Store

# coverage
coverage
*.lcov
lcov.info

# CI workflow staged locally; push needs a workflow-scoped token (add via UI/scoped push)
.github/workflows/ci.yml
.gstack/
