{
  "name": "tsugu",
  "version": "0.1.0",
  "private": true,
  "description": "Tsugu — money that moves on proof, not promises: AI-verified conditional escrow on Somnia",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "contracts:build": "pnpm --filter @asom/contracts build",
    "contracts:test": "pnpm --filter @asom/contracts test"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
